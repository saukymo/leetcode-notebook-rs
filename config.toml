baseURL = '//blog.mkdef.com/leetcode-notebook-rs/'
languageCode = 'zh-hans'
title = 'LeetCode NoteBook'
theme = "hugo-book"
author = "无生"

# https://github.com/gohugoio/hugo/issues/7182
relativeURLs = true
canonifyURLs = true

# Book configuration
disablePathToLower = true

# Needed for mermaid/katex shortcodes
[markup]
[markup.goldmark.renderer]
  unsafe = true

[markup.tableOfContents]
  startLevel = 1

[menu]
[[menu.before]]
  name = "序"
  url = "https://blog.mkdef.com/leetcode-notebook-rs/"
  weight = 10

[[menu.before]]
  name = "Blog"
  url = "https://blog.mkdef.com/"
  weight = 20

[[menu.before]]
  name = "Github"
  url = "https://github.com/saukymo/leetcode-rs"
  weight = 30

[params]
  # (Optional, default light) Sets color theme: light, dark or auto.
  # Theme 'auto' switches between dark and light modes based on browser/os preferences
  BookTheme = 'auto'

  # (Optional, default true) Controls table of contents visibility on right side of pages.
  # Start and end levels can be controlled with markup.tableOfContents setting.
  # You can also specify this parameter per page in front matter.
  BookToC = true

  # (Optional, default docs) Specify section of content to render as menu
  # You can also set value to '*' to render all sections to menu
  BookSection = '*'

  # (Optional, default true) Enables search function with flexsearch,
  # Index is built on fly, therefore it might slowdown your website.
  # Configuration for indexing can be adjusted in i18n folder per language.
  BookSearch = false

  bookCollapseSection = true